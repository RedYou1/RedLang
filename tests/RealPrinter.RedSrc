include "Printer.RedSrc"

class RealPrinter<T> implements Printer<T>{

    T object;

    function RealPrinter(T object){
        this.object = object;
    }

    function void print(T d){
        System.print(this.object);
        System.print(":");
        System.println(d);
    }
}